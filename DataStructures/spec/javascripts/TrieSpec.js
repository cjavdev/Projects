describe("Trie", function() {
	var trie;
	
	beforeEach(function() {
		trie = new Trie();
	});
	
	it("should be able to add a string", function() {
		trie.addWord("string");
    console.log(trie);
	});

  it("should be able to check if a prefix exists",  function() {

  });

  it("should be able to check if the word exists", function() {
 
  });

  it("should return a word, if the word is in the Trie", function() {

  });

  it("should return null if the word is not in the Trie", function() {

  });

  it("should get prefix", function() {

  });

  it("should return if the Trie contains a word", function() {

  });
});

describe("Node", function() {
  var node;

  beforeEach(function() {
    node = new Node("a", "apple");
  });
  
  it("can add a child", function() {

  });
  
  it("can check if one of the children contain that value", function() {

  });

  it("can get child", function() {

  });

  it("can check if it is a word", function() {

  });
});
