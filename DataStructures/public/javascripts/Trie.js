var Trie = function() {
  
};

Trie.prototype.addWord = function(word) {
  console.log(word);
};

Trie.prototype.containsPrefix = function(prefix) {
  console.log(prefix);
};

Trie.prototype.containsWord = function(word) {

};

Trie.prototype.getWord = function(word) {

};

Trie.prototype.getPrefix = function(prefix) {

};

var Node = function(ch, value) {
  this.ch = ch;
  this.value = value;
};

Node.prototype.addChild = function(node) {
  
};

Node.prototype.containsChildValue = function(c) {

};

Node.prototype.getChild = function(c) {

};

Node.prototype.isWord = function() {

};
